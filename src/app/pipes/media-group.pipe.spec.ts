import { MediaGroupPipe } from './media-group.pipe';

describe('MediaGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
